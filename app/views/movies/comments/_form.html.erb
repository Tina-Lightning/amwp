<%= form_for([@movie, @movie.comments.build]) do |f| %>

  <div class="field">
    <%= f.text_area :comment, class: "form-control amwp-text-area" %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-default amwp-button", rows: 3 %>
  </div>
<% end %>